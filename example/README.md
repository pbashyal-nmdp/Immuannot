# Run Example

## A Running example in Docker

Pre-requisites: [Docker](https://docker.com) for your platform.

To run the example without installing any dependencies, try:

```
./docker-run.sh
```


